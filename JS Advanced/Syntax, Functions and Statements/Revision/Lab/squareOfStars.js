function squareOfStars(input = 5) {
    let result;
  for(let i = 1; i <= input; i++) {
    //   console.log(`${'* '.repeat(input)} `);
    console.log('* '.repeat(input));

   }
   
}
squareOfStars(7);