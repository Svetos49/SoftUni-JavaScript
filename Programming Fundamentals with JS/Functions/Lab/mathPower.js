function solve(num, power) {
    //console.log(num ** power);

   /* let sum = num;
    for(let i = 1; i < power; i++) {
      sum *= num;
    }
    console.log(sum); */

    console.log(Math.pow(num, power));
}

solve(2, 8);